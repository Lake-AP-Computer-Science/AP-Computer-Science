
public class FileManager extends Object{
	//print anything works
	//method overloading with different types
	//it is the type of the parameters that is overloading
	//with more than one parameters, order matters	
	
	public void Function() throws Exception //declares this function throws error
	{
		try {;} catch(Exception e){;} //try catch to recover from

		throw new Exception("crash"); //throw it yourself
	}
}
