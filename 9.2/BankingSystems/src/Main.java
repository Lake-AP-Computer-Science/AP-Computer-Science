
public class main {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		CheckingAccount C = new CheckingAccount(); //makes an instance of the blank class
		C.toString(); //the built-in method for Object that we are supposed to know. Returns a string of the name of the class with hashcode (idk what that does tbh)
		C.equals(C);//the built-in method for Object that we are supposed to know. Returns a boolean to indicate if this is equal to the passed in parameter
		
		System.out.println(C); //lake's demo of auto .toString()
		
	}

}
